import  { IconContainer, IconImage } from "./style";

const PokemonIcon = () => {
  return (
    <IconContainer>
      <IconImage src="/pokemon-title.png" alt="pokemon-title" />
    </IconContainer>
  );
};

export default PokemonIcon;
